package com.github.cstroe.spendhawk.dao;

import org.junit.Test;

public class TransactionEntityIT {

    @Test
    public void testFindById() {
//        TransactionDao.findById(1L).orElseThrow(RuntimeException::new);
//        TransactionDao.findById(2L).orElseThrow(RuntimeException::new);
//        TransactionDao.findById(10L).orElseThrow(RuntimeException::new);
//        TransactionDao.findById(12L).orElseThrow(RuntimeException::new);
    }
}
